In order to run the HelloWorld code, it can be copy/pasted into https://snack.expo.io/ and either previewed in the browser itself, or a qr code can be generated through clicking "Run on your device" so the program can be run on an iPhone that installs the Expo app and takes a picture of the qr code through the camera application.
